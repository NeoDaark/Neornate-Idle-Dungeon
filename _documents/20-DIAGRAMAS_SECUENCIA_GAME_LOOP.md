# 📊 Diagramas de Secuencia: Game Loop

Visualización de las interacciones en el game loop con ejemplos concretos.

---

## 1️⃣ Secuencia: Activación de Skill

```
┌─────────────────────────────────────────────────────────────────────┐
│                     User Interface Layer                            │
│                   (MiningSkill.vue Component)                       │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             │ User clicks "Activar Minería"
                             │
                             ▼
                    handleActivateSkill()
                             │
                             │ cycleDuration = 5000
                             │ selectedProduct = mineral-cobre
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     Pinia Store Layer                               │
│                  (skillsStore.ts)                                   │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             │ activateSkill(MINERIA, product, 5000)
                             │
         ┌───────────────────┼───────────────────┐
         │                   │                   │
         ▼                   ▼                   ▼
    Validar     Obtener Tool    Calcular Duración
    (máx 1      Bonus           Final
    activo)     (speedBonus)
         │          │              │
         │          │              │
         └──────────┴──────────────┘
                    │
                    ▼
         state.isActive = true
         state.cycleEndTime = now + finalDuration
         state.currentProduct = product
         state.lastCycleTime = now
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                  Reactivity System (Vue)                            │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             │ activeSkills computed actualizado
                             │ (filtra isActive=true)
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      Game Loop                                      │
│                 (setInterval cada 100ms)                            │
│              En próximo tick (dentro de 100ms)                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 2️⃣ Secuencia: Game Loop en Acción (Ciclo)

```
         TICK #1 (t=0ms)
              │
              ▼
    ┌─────────────────────────┐
    │  Game Loop Comienza     │
    │  Cada 100ms             │
    └──────────┬──────────────┘
               │
               ▼
    ┌─────────────────────────────────────────┐
    │ const activeSkills = skillsStore        │
    │         .activeSkills                   │
    │                                         │
    │ Resultado: [MINERIA_STATE]              │
    │ (solo skills con isActive=true)         │
    └──────────┬──────────────────────────────┘
               │
               ▼
    ┌─────────────────────────────────────────┐
    │ activeSkills.forEach(skill => {         │
    │   const now = Date.now()     // t=0ms   │
    │                                         │
    │   if (skill.cycleEndTime > 0 &&        │
    │       now >= skill.cycleEndTime)        │
    │   {                                     │
    │     // Entrar si se cumple tiempo       │
    │   }                                     │
    │ })                                      │
    └──────────┬──────────────────────────────┘
               │
               ├─ now (0) >= cycleEndTime (5000)? ❌ NO
               │  → Pasar al siguiente skill o salir
               │
               ▼
    ESPERAR 100ms para siguiente tick

         TICK #2 (t=100ms)
              │
              ▼
         [Repetir igual]
         now (100) >= cycleEndTime (5000)? ❌ NO

         ...

         TICK #50 (t=5000ms)
              │
              ▼
    ┌─────────────────────────────────────────┐
    │ now = 5000ms                            │
    │ skill.cycleEndTime = 5000ms             │
    │                                         │
    │ if (5000 >= 5000)  ✅ TRUE              │
    └──────────┬──────────────────────────────┘
               │
               ▼
    ┌─────────────────────────────────────────────────────┐
    │  skillsStore.completeCycle(MINERIA, inventory)     │
    └──────────┬──────────────────────────────────────────┘
               │
        ┌──────┴─────────────────────┬─────────────┐
        │                            │             │
        ▼                            ▼             ▼
    Validar             Procesar        Procesar
    Pre-requisitos      XP + Bonus      Items
        │                  │             │
        ├─ ¿Producto?      │             │
        ├─ ¿inventoryStore?│            │
        ├─ ¿Materiales?    │            │
        │                  │             │
        └──────────────────┴─────────────┘
                   │
                   ├─ Si algo falla → return null ❌
                   │
                   └─ Si todo OK → return result ✅
                                      │
                                      ▼
                   ┌────────────────────────────────┐
                   │ result = {                     │
                   │   skill: MINERIA,              │
                   │   xpGained: 10,                │
                   │   quantity: 1,                 │
                   │   ...                          │
                   │ }                              │
                   └────────────────────────────────┘
                                      │
                                      ▼
    ┌─────────────────────────────────────────────────────┐
    │ if (result && skill.isActive)  ✅ TRUE && TRUE      │
    └──────────┬──────────────────────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────────────────┐
    │ activateSkill(MINERIA, product, 5000)           │
    │                                                  │
    │ → cycleEndTime = now + 5000                      │
    │ → cycleEndTime = 5000 + 5000 = 10000ms          │
    └──────────┬───────────────────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────┐
    │ SKILL REENTERED IN ACTIVE LIST   │
    │ (activeSkills computed update)   │
    └──────────┬───────────────────────┘
               │
               ▼
    ESPERAR 100ms para siguiente tick

         TICK #51 (t=5100ms)
              │
              ▼
         [Repetir: now (5100) >= cycleEndTime (10000)? ❌ NO]
         
         ...
         
         TICK #100 (t=10000ms)
              │
              ▼
         [Repetir: now (10000) >= cycleEndTime (10000)? ✅ SÍ]
         [Se ejecuta completeCycle() nuevamente]
```

---

## 3️⃣ Secuencia: Fallo por Materiales Insuficientes

```
         Suponiendo:
         - HERRERIA activo con producto que requiere 10 lingotes
         - Inventario tiene solo 5 lingotes

         TICK #N (Cuando se cumple cycleEndTime)
              │
              ▼
    ┌─────────────────────────────────┐
    │ completeCycle(HERRERIA)         │
    └──────────┬──────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────────────┐
    │ Validar materiales:                          │
    │   for (material of requiredMaterials) {      │
    │     available = getItemQuantity('lingote')   │
    │     // = 5                                   │
    │     if (5 < 10)  ✅ TRUE                     │
    │       console.warn('No hay suficientes')     │
    │       return null  ❌                        │
    │   }                                          │
    └──────────┬───────────────────────────────────┘
               │
               ▼ result = null
               
    ┌─────────────────────────────────────────────────────┐
    │ if (result && skill.isActive)                       │
    │   → (null && true) = false  → NO ENTRAR            │
    │                                                     │
    │ else if (!result && skill.isActive)                 │
    │   → (true && true) = true  → ENTRAR                │
    └──────────┬────────────────────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────────┐
    │ deactivateSkill(HERRERIA, true)          │
    │   // true = preservar cycleEndTime       │
    │                                          │
    │ state.isActive = false                   │
    │ state.cycleEndTime → PRESERVADO          │
    │ state.currentProduct → PRESERVADO        │
    └──────────┬───────────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────────┐
    │ activeSkills.computed se actualiza       │
    │ HERRERIA desaparece de activeSkills      │
    │ (ya que isActive=false)                  │
    └──────────┬───────────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────────┐
    │ UI Component muestra: "PAUSADO"          │
    │ (porque isActive=false)                  │
    │                                          │
    │ User agrega 5 lingotes más               │
    │ → Ahora tiene 10 lingotes                │
    │                                          │
    │ User clickea "Continuar" o "Reactivar"  │
    └──────────┬───────────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────────┐
    │ activateSkill(HERRERIA, product, 5000)  │
    │                                          │
    │ → VUELVE A activeSkills                  │
    │ → Continúa normalmente                   │
    └──────────────────────────────────────────┘
```

---

## 4️⃣ Secuencia: Offline Processing

```
         APP SE CIERRA
         t = 13:00:00 (lastActiveTime guardado)
         
         MINERIA activo con:
         - cycleEndTime = 13:02:00
         - currentProduct = mineral-cobre
         
              │
              ▼ (10 minutos después)
              
         APP SE ABRE
         t = 13:10:00 (now)
         
              │
              ▼
    ┌─────────────────────────────────────────┐
    │ onMounted ejecutado                     │
    └──────────┬──────────────────────────────┘
               │
               ▼
    ┌─────────────────────────────────────────┐
    │ gameStore.initializeGame()              │
    │ → Cargar todos los stores desde local   │
    │   - cycleEndTime se restaura            │
    │   - currentProduct se restaura          │
    │   - isActive = true aún                 │
    └──────────┬──────────────────────────────┘
               │
               ▼
    ┌─────────────────────────────────────────┐
    │ setTimeout(3000ms) → Simulación carga   │
    └──────────┬──────────────────────────────┘
               │
               ▼
    ┌─────────────────────────────────────────────────────┐
    │ gameStore.calculateOfflineProgress()                │
    │                                                     │
    │ lastActiveTime = 13:00:00                          │
    │ now = 13:10:00                                     │
    │ offlineMs = 600,000ms (10 minutos)                 │
    └──────────┬──────────────────────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────────────────────┐
    │ for each skillState:                                 │
    │   if (!skillState.currentProduct) skip               │
    │   else:                                              │
    │     baseCycleDuration = 5000ms                       │
    │     cycleTime = 5000ms (sin bonuses)                │
    └──────────┬───────────────────────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────────────────────────┐
    │ Calcular ciclos completados:                             │
    │                                                          │
    │ timeUntilCycleCompletes = cycleEndTime - lastActiveTime │
    │                         = 13:02 - 13:00                 │
    │                         = 120,000ms (2 minutos)         │
    │                                                          │
    │ offlineMs (600,000) >= timeUntilCycleCompletes (120k)? │
    │   ✅ YES                                                │
    │                                                          │
    │ cyclesCompleted = 1 (el ciclo en progreso)             │
    │ timeAfterFirstComplete = 600,000 - 120,000 = 480,000  │
    │ additionalCycles = floor(480,000 / 5000) = 96         │
    │ cyclesCompleted TOTAL = 1 + 96 = 97 ciclos            │
    └──────────┬───────────────────────────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────────────────────────┐
    │ Procesar 97 ciclos:                                      │
    │                                                          │
    │ for (i = 0; i < 97; i++) {                             │
    │   completeCycle(MINERIA, inventory, false)             │
    │   ├─ Validar materiales (todos disponibles)            │
    │   ├─ Agregar 1 mineral                                 │
    │   ├─ Agregar 10 XP                                     │
    │   └─ false = NO resetear cycleEndTime                  │
    │ }                                                       │
    │                                                          │
    │ totalQuantity = 97 * 1 = 97 minerales                 │
    │ totalXP = 97 * 10 = 970 XP                            │
    └──────────┬───────────────────────────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────────────────────────┐
    │ Actualizar cycleEndTime para nuevos ciclos:             │
    │                                                          │
    │ timeUsedByCompletedCycles = 97 * 5000 = 485,000ms      │
    │ timeIntoCycleAfterComplete = 600,000 - 485,000         │
    │                            = 115,000ms                 │
    │                                                          │
    │ cycleEndTime = now + max(0, 5000 - 115,000)           │
    │             = 13:10:00 + 0                             │
    │             = 13:10:00 (comienza nuevo ciclo)          │
    └──────────┬───────────────────────────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────────────────────────┐
    │ offlineHarvestSummary = {                               │
    │   totalOfflineMs: 600,000,                              │
    │   skillHarvests: [{                                     │
    │     skill: 'mineria',                                  │
    │     cyclesCompleted: 97,                              │
    │     totalQuantity: 97,                                 │
    │     totalXP: 970                                        │
    │   }]                                                    │
    │ }                                                       │
    └──────────┬───────────────────────────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────────────────────────┐
    │ saveGame() → Guardar estado actualizado a localStorage  │
    └──────────┬───────────────────────────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────────────────────────┐
    │ isLoading = false → Mostrar layout                      │
    │                                                          │
    │ OfflineHarvestNotification muestra:                     │
    │ "97 ciclos completados offline!"                       │
    │ "Ganaste: 97 minerales, 970 XP"                        │
    └──────────┬───────────────────────────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────────────────────────┐
    │ startGameLoop() → Game loop activo comienza             │
    │                  con cycleEndTime = 13:10:00           │
    │                  (comienza ciclo fresco)               │
    └──────────────────────────────────────────────────────────┘
```

---

## 5️⃣ Timeline: Ejemplo Concreto (Minería)

```
REALIDAD REAL:

t=0s      : User activa MINERIA
           activateSkill()
           cycleEndTime = 0s + 5000ms = 5000ms

t=0.1s    : Game loop tick #1
           now (0.1s) < cycleEndTime (5s) → Skip

t=0.2s    : Game loop tick #2
           now (0.2s) < cycleEndTime (5s) → Skip

...

t=5.0s    : Game loop tick #50
           now (5s) >= cycleEndTime (5s) → ✅ EJECUTAR
           completeCycle() → +1 mineral, +10 XP
           activateSkill() nuevamente
           cycleEndTime = 5000ms + 5000ms = 10000ms

t=5.1s    : Game loop tick #51
           now (5.1s) < cycleEndTime (10s) → Skip

...

t=10.0s   : Game loop tick #100
           now (10s) >= cycleEndTime (10s) → ✅ EJECUTAR
           completeCycle() → +1 mineral, +10 XP
           activateSkill() nuevamente
           cycleEndTime = 10000ms + 5000ms = 15000ms

...

[Se repite cada 5 segundos indefinidamente]

RESULTADO DESPUÉS DE 1 MINUTO:
- ciclos completados = 60,000ms / 5000ms = 12
- minerales: 12
- XP: 120


OFFLINE EXAMPLE:

t=0s-60s  : Minería activa como arriba
           cycleEndTime = 60,000ms

t=60s     : App se cierra
           lastActiveTime = 1708346060000

t=60s+600s: App reabre (600 segundos = 10 minutos después)
           now = 1708346660000
           offlineMs = 600,000ms

Cálculo:
  timeUntilCycleCompletes = 60,000 - 0 = 60,000ms
  offlineMs (600,000) >= 60,000? ✅ YES
  
  cyclesCompleted = 1
  timeAfterFirstComplete = 600,000 - 60,000 = 540,000ms
  additionalCycles = floor(540,000 / 5000) = 108
  cyclesCompleted TOTAL = 1 + 108 = 109 ciclos

RESULTADO OFFLINE:
- ciclos completados: 109
- minerales: 109
- XP: 1,090
```

---

## 6️⃣ Estados de un Skill Activo

```
                    ┌─────────────────┐
                    │   INACTIVE      │
                    │  (isActive=false)
                    └────────┬────────┘
                             │
                    User clicks activar
                             │
                             ▼
                    ┌─────────────────┐
                    │   ACTIVE        │
                    │  (isActive=true)│
                    │  cycleEndTime>0 │
                    └────────┬────────┘
                             │
            ┌────────────────┼────────────────┐
            │                │                │
          TICK             OFFLINE         USER
         reaches          calculate      pause
         cycleEndTime      cycles      request
            │                │            │
            ▼                ▼            ▼
        ┌──────────┐   ┌──────────┐  ┌──────────┐
        │COMPLETE  │   │COMPLETE  │  │ PAUSED   │
        │CYCLE     │   │OFFLINE   │  │(preserved)
        │          │   │          │  │          │
        │+ Item    │   │+ XP/Items│  │cycleEnd- │
        │+ XP      │   │Update    │  │Time kept │
        └────┬─────┘   │cycleEnd  │  └────┬─────┘
             │         └──────┬──┘        │
             │                │          │
      Re-activate        resumeWork     Manual
      automatically      or manual      resume
             │            resume         │
             └────────────┬─────────────┘
                          │
                          ▼
                    ┌─────────────────┐
                    │   ACTIVE AGAIN  │
                    │  (isActive=true)│
                    │ cycleEndTime>0  │
                    └────────┬────────┘
                             │
                          [LOOP]
```

---

**Referencia**: Ver documento 18-ANALISIS_GAME_LOOP.md para detalles técnicos
